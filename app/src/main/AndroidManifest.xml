<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmokeMate"
        android:configChanges="locale|orientation|screenSize"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SmokeMate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="registrastion.activities.RegistrationActivity">

        </activity>

        <activity android:name="registrastion.activities.RegistrationSecondActivity">

        </activity>

        <activity android:name="registrastion.activities.RegistrationConfirmActivity">

        </activity>

        <activity android:name="forgot.password.activities.ForgotPasswordPhoneActivity">

        </activity>

        <activity android:name="forgot.password.activities.ForgotPasswordConfirmActivity">

        </activity>

        <activity android:name="forgot.password.activities.ForgotPasswordNewActivity">

        </activity>

    </application>

</manifest>